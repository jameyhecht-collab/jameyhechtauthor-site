<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Request Full Book Proposal – Into Theism</title>
<style>
:root {
--bg: #0b0c10;
--card: #121318;
--text: #e6e6e6;
--muted: #b7bbc5;
--accent: #4cc9f0;
--accent-2: #80ed99;
--ring: rgba(76, 201, 240, 0.4);
--danger: #ff6b6b;
--shadow: 0 10px 24px rgba(0,0,0,.35);
--radius: 14px;
}
html, body { height: 100%; }
body {
margin: 0; font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji";
background: radial-gradient(1200px 800px at 10% 0%, #0f1016 10%, var(--bg) 70%);
color: var(--text);
}
.wrap { max-width: 880px; margin: 0 auto; padding: 48px 20px 96px; }
.card { background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01)); border: 1px solid rgba(255,255,255,0.08); border-radius: var(--radius); box-shadow: var(--shadow); padding: 28px; }
h1 { font-size: clamp(28px, 3vw, 40px); margin: 0 0 10px; letter-spacing: .3px; }
p.lead { color: var(--muted); margin: 0 0 24px; }
.btn { display: inline-flex; align-items: center; gap: 10px; padding: 14px 18px; border-radius: 14px; border: 1px solid rgba(255,255,255,0.12); background: linear-gradient(180deg, rgba(76,201,240,.18), rgba(76,201,240,.12)); color: #eaf6ff; text-decoration: none; font-weight: 600; cursor: pointer; transition: transform .06s ease, box-shadow .2s ease, background .2s ease; box-shadow: 0 8px 24px rgba(76,201,240,.14); }
.btn:hover { transform: translateY(-1px); box-shadow: 0 10px 28px rgba(76,201,240,.2); }
.btn:focus-visible { outline: 3px solid var(--ring); outline-offset: 2px; }
.micro { font-size: 13px; color: var(--muted); margin-top: 10px; }
.row { display: grid; grid-template-columns: 1.2fr .8fr; gap: 22px; }
@media (max-width: 800px){ .row { grid-template-columns: 1fr; } }
.list { margin: 14px 0 0 0; padding-left: 20px; color: var(--muted); }
/* Contact */
#contact { margin-top: 48px; }
form { display: grid; gap: 14px; background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01)); border: 1px solid rgba(255,255,255,0.08); border-radius: var(--radius); padding: 18px; }
label { font-weight: 600; font-size: 14px; color: #eaeaea; }
input[type="text"], input[type="email"], textarea { width: 100%; border-radius: 12px; border: 1px solid rgba(255,255,255,0.12); background: #0f1116; color: var(--text); padding: 12px 14px; font-size: 16px; }
textarea { min-height: 120px; resize: vertical; }
.actions { display: flex; gap: 10px; align-items: center; flex-wrap: wrap; }
.btn.secondary { background: linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.02)); border-color: rgba(255,255,255,.18); box-shadow: 0 6px 18px rgba(0,0,0,.24); }
.note { font-size: 13px; color: var(--muted); }
.ok { color: var(--accent-2); font-weight: 700; }
.error { color: var(--danger); font-weight: 700; }
code { background: #0e1117; border: 1px solid rgba(255,255,255,0.08); padding: 2px 8px; border-radius: 8px; }
</style>
</head>
<body>
<div class="wrap">
<div class="card">
<h1>Request the Full Book Proposal</h1>
<p class="lead">One click opens your email client with a pre‑filled subject & message. If that doesn’t work on your device, use the contact form below.</p>


<div class="row">
<div>
<!-- Primary CTA: mailto launcher -->
<button id="requestBtn" class="btn" type="button" aria-describedby="cta-help">
📧 Request Full Proposal
</button>
<div id="cta-help" class="micro">Having trouble? <a href="#contact">Use the contact form</a> or <button class="btn secondary" id="copyEmailBtn" type="button">Copy email address</button></div>


<ul class="list">
<li>Pre‑filled <code>Subject</code>: <em>Request for Full Book Proposal — <span id="titlePreview"></span></em></li>
</html>